{
	"name": "cart-parser",
	"version": "0.1.0",
	"private": true,
	"dependencies": {
		"react-scripts": "5.0.1",
		"uuid": "^9.0.1"
	},
	"scripts": {
		"test": "react-scripts test --env=jsdom --coverage",
		"test:debug": "NODE_OPTIONS='--experimental-vm-modules --inspect-brk' react-scripts test --env=jsdom"
	},
	"engines": {
		"node": ">=16",
		"npm": ">=8"
	}
}
